# Module 4: Metadata Extraction

## Complexity Rating

⚠️ **Medium** — 多文件协调修改（1个新服务 + 6个后端文件 + 2个前端文件），需按序执行。

---

## Architecture Overview

```
Upload → import_service.process_document()
             ↓
         extract_text() → chunk → embed → store chunks
             ↓
         metadata_service.extract_metadata(text, filename)  ← NEW
             ↓ (LLM structured output → DocumentMetadata Pydantic model)
         documents.update({metadata: {...}, status: "complete"})

Chat → LLM calls retrieve_documents(query, language?, document_type?)
             ↓
         retrieve_chunks(query, user_id, supabase, filters={...})  ← EXTENDED
             ↓
         match_chunks_filtered SQL function (JOINs documents for metadata filter)
```

---

## Execution Order

1. Run `004_add_metadata.sql` in Supabase SQL Editor
2. Create `metadata_service.py`
3. Update `import_service.py` (add `filename` param, call `extract_metadata`)
4. Update `documents.py` router (pass `filename` to background task)
5. Update `retrieval_service.py` (add `filters` param)
6. Update `llm_service.py` (extend RETRIEVAL_TOOL schema)
7. Update `chat.py` (parse filters, pass to `retrieve_chunks`)
8. Update `models/document.py` (add `metadata` field)
9. Update `frontend/src/types/index.ts`
10. Update `frontend/src/components/import/DocumentList.tsx`

---

## Step 1 — `supabase/migrations/004_add_metadata.sql`

```sql
-- Module 4: Structured metadata extraction
ALTER TABLE documents
  ADD COLUMN IF NOT EXISTS metadata JSONB;

CREATE INDEX IF NOT EXISTS idx_documents_metadata
  ON documents USING GIN (metadata);

CREATE OR REPLACE FUNCTION match_chunks_filtered(
  query_embedding    vector(1536),
  match_user_id      UUID,
  match_count        INT     DEFAULT 5,
  match_threshold    FLOAT   DEFAULT 0.7,
  filter_language    TEXT    DEFAULT NULL,
  filter_document_type TEXT  DEFAULT NULL
)
RETURNS TABLE (
  id          UUID,
  document_id UUID,
  content     TEXT,
  chunk_index INT,
  similarity  FLOAT
)
LANGUAGE SQL STABLE AS $$
  SELECT
    c.id,
    c.document_id,
    c.content,
    c.chunk_index,
    1 - (c.embedding <=> query_embedding) AS similarity
  FROM chunks c
  JOIN documents d ON d.id = c.document_id
  WHERE c.user_id = match_user_id
    AND c.embedding IS NOT NULL
    AND 1 - (c.embedding <=> query_embedding) >= match_threshold
    AND (filter_language      IS NULL OR d.metadata->>'language'      = filter_language)
    AND (filter_document_type IS NULL OR d.metadata->>'document_type' = filter_document_type)
  ORDER BY c.embedding <=> query_embedding
  LIMIT match_count;
$$;
```

---

## Step 2 — `backend/app/services/metadata_service.py` (NEW)

```python
from typing import Literal
from pydantic import BaseModel
from app.services.llm_service import get_llm_client
from app.config import settings


class DocumentMetadata(BaseModel):
    title: str
    summary: str
    language: str          # ISO 639-1 code, e.g. "en", "zh"
    topics: list[str]      # 3-7 short topic tags
    document_type: Literal["article", "manual", "report", "other"]


_MAX_CHARS = 4000

_EXTRACTION_PROMPT = (
    "You are a document analyst. Extract structured metadata from the document excerpt below.\n\n"
    "Rules:\n"
    "- title: infer from content if not explicit; use the filename as fallback\n"
    "- summary: 1-2 sentences describing what the document is about\n"
    "- language: ISO 639-1 two-letter code (e.g. 'en', 'zh', 'fr')\n"
    "- topics: 3 to 7 short lowercase topic tags\n"
    "- document_type: one of 'article', 'manual', 'report', 'other'\n\n"
    "Respond ONLY with valid JSON matching the schema. No explanation."
)


def extract_metadata(text: str, filename: str) -> DocumentMetadata:
    client = get_llm_client()
    excerpt = text[:_MAX_CHARS]
    user_message = f"Filename: {filename}\n\nDocument excerpt:\n{excerpt}"

    response = client.beta.chat.completions.parse(
        model=settings.LLM_MODEL,
        messages=[
            {"role": "system", "content": _EXTRACTION_PROMPT},
            {"role": "user", "content": user_message},
        ],
        response_format=DocumentMetadata,
    )
    return response.choices[0].message.parsed
```

---

## Step 3 — `backend/app/services/import_service.py`

Changes: add `filename` param, call `extract_metadata`, store in DB.

```python
from supabase import Client
from app.services.chunking_service import chunk_text
from app.services.embedding_service import embed_texts
from app.services.metadata_service import extract_metadata


def _extract_text(content: bytes, file_type: str) -> str:
    if file_type in ("txt", "md"):
        return content.decode("utf-8", errors="replace")
    raise ValueError(f"Unsupported file type: {file_type}")


def process_document(
    document_id: str,
    user_id: str,
    file_content: bytes,
    file_type: str,
    filename: str,          # NEW in Module 4
    supabase: Client,
) -> None:
    def _set_status(status: str, error_msg: str | None = None):
        update: dict = {"status": status}
        if error_msg:
            update["error_msg"] = error_msg
        supabase.table("documents").update(update).eq("id", document_id).execute()

    try:
        _set_status("processing")

        text = _extract_text(file_content, file_type)
        chunks = chunk_text(text)

        texts = [c.content for c in chunks]
        embeddings = embed_texts(texts)

        rows = [
            {
                "document_id": document_id,
                "user_id": user_id,
                "content": chunk.content,
                "chunk_index": chunk.chunk_index,
                "token_count": chunk.token_count,
                "embedding": embedding,
            }
            for chunk, embedding in zip(chunks, embeddings)
        ]

        BATCH = 100
        for i in range(0, len(rows), BATCH):
            supabase.table("chunks").insert(rows[i:i + BATCH]).execute()

        # Module 4: extract structured metadata via LLM
        try:
            metadata = extract_metadata(text, filename)
            metadata_dict = metadata.model_dump()
        except Exception:
            metadata_dict = None  # extraction failed; still mark complete

        supabase.table("documents").update({
            "status": "complete",
            "chunk_count": len(chunks),
            "metadata": metadata_dict,
        }).eq("id", document_id).execute()

    except Exception as e:
        _set_status("error", error_msg=str(e))
        raise
```

---

## Step 4 — `backend/app/routers/documents.py`

Only change: add `filename=file.filename` to both `background_tasks.add_task()` calls.

---

## Step 5 — `backend/app/services/retrieval_service.py`

```python
from dataclasses import dataclass
from supabase import Client
from app.services.embedding_service import embed_texts


@dataclass
class RetrievalResult:
    chunk_id: str
    document_id: str
    content: str
    chunk_index: int
    similarity: float


def retrieve_chunks(
    query: str,
    user_id: str,
    supabase: Client,
    match_count: int = 5,
    match_threshold: float = 0.7,
    filters: dict | None = None,
) -> list[RetrievalResult]:
    embedding = embed_texts([query])[0]
    active_filters = {k: v for k, v in (filters or {}).items() if v is not None}

    if active_filters:
        result = supabase.rpc("match_chunks_filtered", {
            "query_embedding": embedding,
            "match_user_id": user_id,
            "match_count": match_count,
            "match_threshold": match_threshold,
            "filter_language": active_filters.get("language"),
            "filter_document_type": active_filters.get("document_type"),
        }).execute()
    else:
        result = supabase.rpc("match_chunks", {
            "query_embedding": embedding,
            "match_user_id": user_id,
            "match_count": match_count,
            "match_threshold": match_threshold,
        }).execute()

    return [
        RetrievalResult(
            chunk_id=row["id"],
            document_id=row["document_id"],
            content=row["content"],
            chunk_index=row["chunk_index"],
            similarity=row["similarity"],
        )
        for row in (result.data or [])
    ]
```

---

## Step 6 — `backend/app/services/llm_service.py`

Replace `RETRIEVAL_TOOL` constant only:

```python
RETRIEVAL_TOOL = {
    "type": "function",
    "function": {
        "name": "retrieve_documents",
        "description": (
            "Search the user's imported documents for information relevant to the query. "
            "Use this tool when the user's question might be answered by their uploaded documents. "
            "Returns text excerpts ranked by relevance. "
            "Optionally filter by language (ISO 639-1 code, e.g. 'en', 'zh') or "
            "document_type ('article', 'manual', 'report', 'other') when the user "
            "explicitly requests a specific language or document type."
        ),
        "parameters": {
            "type": "object",
            "properties": {
                "query": {
                    "type": "string",
                    "description": "The search query to find relevant document excerpts",
                },
                "language": {
                    "type": "string",
                    "description": (
                        "ISO 639-1 language code to filter results "
                        "(e.g. 'en', 'zh'). Only set when user specifies a language."
                    ),
                },
                "document_type": {
                    "type": "string",
                    "enum": ["article", "manual", "report", "other"],
                    "description": (
                        "Filter to documents of this type. "
                        "Only set when user specifies a document type."
                    ),
                },
            },
            "required": ["query"],
        },
    },
}
```

---

## Step 7 — `backend/app/routers/chat.py`

In the `tool_calls` branch, replace:
```python
results = retrieve_chunks(query, user["id"], supabase)
```
With:
```python
filters = {
    "language": args.get("language"),
    "document_type": args.get("document_type"),
}
results = retrieve_chunks(query, user["id"], supabase, filters=filters)
```
And update the `except` block to also set `filters = {}`.

---

## Step 8 — `backend/app/models/document.py`

Add `metadata: Optional[dict[str, Any]] = None` field. Also add `Any` to imports.

---

## Step 9 — `frontend/src/types/index.ts`

Add:
```typescript
export interface DocumentMetadata {
  title: string
  summary: string
  language: string
  topics: string[]
  document_type: 'article' | 'manual' | 'report' | 'other'
}
```
And add `metadata: DocumentMetadata | null` to `Document` interface.

---

## Step 10 — `frontend/src/components/import/DocumentList.tsx`

For completed documents with metadata, show chips below the filename:
- Blue chip: language code
- Purple chip: document_type
- Grey chips: up to 4 topics

---

## Validation Tests

1. **Migration**: `metadata` column (JSONB) and `match_chunks_filtered` function exist in Supabase
2. **Metadata stored**: After upload completes, `SELECT metadata FROM documents` returns valid JSON
3. **Frontend displays**: Language/type/topic chips appear on complete document cards
4. **Unfiltered retrieval**: Questions without language/type spec use original `match_chunks`
5. **Filtered retrieval**: "Search my English documents for X" triggers `match_chunks_filtered` with `filter_language='en'`
6. **Legacy docs**: Documents with `metadata=NULL` still render correctly in the UI
7. **Realtime update**: Metadata chips appear without page reload after processing completes

---

## Notes

- `client.beta.chat.completions.parse()` requires `openai>=1.30.0`
- Metadata extraction failure is non-fatal (falls back to `null`, document still marked complete)
- Existing `match_chunks` function unchanged — backward compatible
